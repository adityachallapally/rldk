# Development dependencies with pinned versions.
# Install optional stacks with: `pip install -e .[compiled]` (or other extras).

# Core runtime pins
-r requirements.txt

# Testing
pytest==7.4.0
pytest-cov==4.1.0

# Code formatting and linting
black==23.12.0
isort==5.13.0
mypy==1.8.0
ruff==0.1.0

# Property-based testing
hypothesis==6.92.0
